# 贡献指南

感谢你考虑为 Horse 发生项目做出贡献！

## 📋 目录

- [行为准则](#行为准则)
- [如何贡献](#如何贡献)
- [开发流程](#开发流程)
- [代码规范](#代码规范)
- [提交规范](#提交规范)
- [问题反馈](#问题反馈)

---

## 行为准则

### 我们的承诺

为了营造一个开放和友好的环境，我们承诺：

- 使用友好和包容的语言
- 尊重不同的观点和经验
- 优雅地接受建设性批评
- 关注对社区最有利的事情
- 对其他社区成员表示同理心

### 不可接受的行为

- 使用性化的语言或图像
- 侮辱性/贬损性评论，人身攻击或政治攻击
- 公开或私下骚扰
- 未经明确许可，发布他人的私人信息
- 其他在专业环境中可能被认为不适当的行为

---

## 如何贡献

### 报告 Bug

在提交 Bug 报告之前：

1. **检查现有 Issues** - 确保问题尚未被报告
2. **使用最新版本** - 确认问题在最新版本中仍然存在
3. **收集信息** - 准备详细的复现步骤

**Bug 报告应包含**:

```markdown
**描述**
简要描述问题

**复现步骤**
1. 执行 '...'
2. 点击 '...'
3. 看到错误

**期望行为**
描述你期望发生什么

**实际行为**
描述实际发生了什么

**环境**
- OS: [e.g. Ubuntu 20.04]
- Node.js: [e.g. 18.0.0]
- 浏览器: [e.g. Chrome 120]

**截图**
如果适用，添加截图

**日志**
相关的错误日志
```

### 建议新功能

在提交功能建议之前：

1. **检查现有 Issues** - 确保功能尚未被建议
2. **考虑范围** - 功能是否符合项目目标
3. **提供细节** - 详细描述功能和用例

**功能建议应包含**:

```markdown
**功能描述**
清晰简洁地描述你想要的功能

**问题**
这个功能解决了什么问题？

**解决方案**
描述你希望如何实现

**替代方案**
描述你考虑过的其他方案

**额外信息**
其他相关信息或截图
```

---

## 开发流程

### 1. Fork 项目

点击 GitHub 页面右上角的 "Fork" 按钮

### 2. 克隆仓库

```bash
git clone https://github.com/your-username/horse-app.git
cd horse-app
```

### 3. 创建分支

```bash
git checkout -b feature/your-feature-name
# 或
git checkout -b fix/your-bug-fix
```

**分支命名规范**:
- `feature/` - 新功能
- `fix/` - Bug 修复
- `docs/` - 文档更新
- `refactor/` - 代码重构
- `test/` - 测试相关
- `chore/` - 构建/工具相关

### 4. 安装依赖

```bash
# 前端依赖
npm install

# 服务器依赖
cd server
npm install
```

### 5. 开发

```bash
# 前端开发
npm run dev

# 服务器开发
cd server
npm run dev
```

### 6. 测试

```bash
# 运行测试
npm test

# 检查代码风格
npm run lint

# 修复代码风格
npm run lint:fix
```

### 7. 提交更改

```bash
git add .
git commit -m "feat: add new feature"
```

### 8. 推送到 GitHub

```bash
git push origin feature/your-feature-name
```

### 9. 创建 Pull Request

1. 访问你的 Fork 页面
2. 点击 "New Pull Request"
3. 填写 PR 描述
4. 等待审核

---

## 代码规范

### JavaScript/TypeScript

```javascript
// ✅ 好的示例
function calculateTotal(items) {
  return items.reduce((sum, item) => sum + item.price, 0);
}

// ❌ 不好的示例
function calc(i) {
  let s = 0;
  for(let x of i) s += x.price;
  return s;
}
```

**规则**:
- 使用有意义的变量名
- 函数应该简短且专注
- 添加必要的注释
- 避免深层嵌套
- 使用 ES6+ 语法

### 文件组织

```
src/
├── components/     # React 组件
├── hooks/         # 自定义 Hooks
├── utils/         # 工具函数
├── constants/     # 常量
└── types/         # TypeScript 类型
```

### 命名规范

- **文件名**: `kebab-case.js`
- **组件**: `PascalCase.tsx`
- **函数**: `camelCase()`
- **常量**: `UPPER_SNAKE_CASE`
- **类**: `PascalCase`

---

## 提交规范

使用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

### 格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Type 类型

- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式（不影响代码运行）
- `refactor`: 重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建/工具相关
- `revert`: 回滚

### 示例

```bash
# 新功能
git commit -m "feat(api): add OG image generation endpoint"

# Bug 修复
git commit -m "fix(server): resolve memory leak in image processing"

# 文档
git commit -m "docs(readme): update installation instructions"

# 重构
git commit -m "refactor(utils): simplify date formatting logic"
```

### 详细提交

```bash
git commit -m "feat(api): add rate limiting

- Add express-rate-limit middleware
- Configure 100 requests per 15 minutes
- Add rate limit headers to response

Closes #123"
```

---

## Pull Request 规范

### PR 标题

使用与提交信息相同的格式：

```
feat(api): add new endpoint for user data
```

### PR 描述模板

```markdown
## 描述
简要描述这个 PR 做了什么

## 类型
- [ ] Bug 修复
- [ ] 新功能
- [ ] 文档更新
- [ ] 代码重构
- [ ] 性能优化

## 变更内容
- 添加了 X 功能
- 修复了 Y 问题
- 优化了 Z 性能

## 测试
- [ ] 本地测试通过
- [ ] 添加了单元测试
- [ ] 添加了集成测试

## 截图
如果适用，添加截图

## 相关 Issue
Closes #123
```

### PR 检查清单

提交 PR 前确保：

- [ ] 代码遵循项目规范
- [ ] 添加了必要的测试
- [ ] 所有测试通过
- [ ] 更新了相关文档
- [ ] 提交信息符合规范
- [ ] 没有合并冲突
- [ ] 代码已经过自我审查

---

## 代码审查

### 审查者指南

审查时关注：

1. **功能性** - 代码是否按预期工作
2. **可读性** - 代码是否易于理解
3. **可维护性** - 代码是否易于维护
4. **性能** - 是否有性能问题
5. **安全性** - 是否有安全隐患
6. **测试** - 测试是否充分

### 审查反馈

**好的反馈**:
```
建议: 这里可以使用 Array.map() 来简化代码
理由: 更简洁，性能更好
示例: items.map(item => item.id)
```

**不好的反馈**:
```
这段代码写得不好
```

---

## 问题反馈

### 提问前

1. 搜索现有 Issues
2. 查看文档
3. 尝试调试

### 提问时

提供以下信息：

- 问题描述
- 复现步骤
- 期望结果
- 实际结果
- 环境信息
- 相关日志

---

## 开发环境设置

### 必需工具

- Node.js >= 16.0.0
- npm >= 8.0.0
- Git

### 推荐工具

- VS Code
- ESLint 插件
- Prettier 插件

### VS Code 配置

```json
{
  "editor.formatOnSave": true,
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  }
}
```

---

## 发布流程

### 版本号

遵循 [语义化版本](https://semver.org/):

- `MAJOR.MINOR.PATCH`
- 例如: `1.2.3`

### 发布步骤

1. 更新版本号
2. 更新 CHANGELOG.md
3. 创建 Git tag
4. 推送到 GitHub
5. 创建 Release

---

## 获取帮助

- **文档**: 查看 [README.md](README.md)
- **Issues**: 搜索或创建 Issue
- **讨论**: GitHub Discussions
- **邮箱**: support@goodhorse.fun

---

## 致谢

感谢所有贡献者！

你的贡献让这个项目变得更好 ❤️

---

**记住**: 没有贡献太小，每个贡献都很重要！
